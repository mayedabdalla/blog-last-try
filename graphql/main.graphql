query posts {
    posts {
        slug
        id
        title
        createdAt
    }
}

query post($slug: String) {
    post(slug: $slug){
        slug
        id
        title
        content
        createdAt
    }
}